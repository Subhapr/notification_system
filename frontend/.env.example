# Base URL of the Django backend API (no trailing slash).
# Local dev default; point this at your Render URL in production.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Public OneSignal App ID (safe to expose client-side).
# The REST API key stays server-side only, in the Django backend.
NEXT_PUBLIC_ONESIGNAL_APP_ID=
